---
title: "Aula 8a - Econometria de Séries Temporais"
author: "João Ricardo F. de Lima"
date: "today"
editor: source
lang: pt
language: 
  toc-title-document: '<a href="https://www.facape.br/" target="_blank"><img src="https://github.com/econfacape/macroeconometria/blob/main/logofacape.jpg?raw=true" alt="Logotipo Facape" width="150"></a>'
format: 
  html:
    toc: true
    toc_float:
      collapsed: yes
      smooth_scroll: yes
    toc-location: left
    code-fold: false
    embed-resources: true
    page-layout: full
    fig-width: 12
    fig-height: 6
    fig-align: center
    df-print: paged
    fontsize: 13pt
theme:
  light: flatly
execute:
  echo: TRUE
  message: false
  warning: false
  cache: false
jupyter: python3
---

## Introdução

O objetivo é estimar modelos de previsão de manga palmer, ao produtor do Vale do São Francisco e verificar qual deles possui a maior eficácia. 

```{python}
# Bibliotecas
import pandas as pd
import plotnine as p9
from great_tables import GT, md, html, style, loc
import pmdarima as pmd
import xgboost as xgb
#from nixtla import NixtlaClient
from sklearn.metrics import mean_absolute_error, mean_squared_error
import numpy as np
from pmdarima.preprocessing import FourierFeaturizer
from pmdarima.arima import ARIMA
from statsmodels.tsa.statespace.sarimax import SARIMAX

# Load mango price dataset and drop specified columns
df = (
    pd.read_csv('dados/dados_manga_palmer_semana.csv', sep=';', decimal='.')
    .drop(columns=['produto', 'regiao', 'ano', 'semana'])
)

# Create the date range from 2012-01-07 to today
date_range = pd.date_range(start='2012-01-01', end=pd.Timestamp.today(), freq='W-SAT')

# Remove the specific dates (53rd weeks)
dates_to_remove = [pd.Timestamp('2016-12-31'), pd.Timestamp('2022-12-31')]
date_range = date_range[~date_range.isin(dates_to_remove)]

# Assign the date range as a new column
df['date'] = date_range[:len(df)]
# Assign the date range as a new column
df['date2'] = date_range[:len(df)]

# Set the date as the index
df = df.set_index('date')

# Load igpdi dataset
igpdi = (
    pd.read_csv('dados/igpdi.csv', sep=';', decimal='.')
    .assign(date=df.index)
    .drop(columns=['ano', 'semana'])
    .set_index('date')
)

# Merge DataFrames
tabela = df.join(igpdi, how='inner')

# Deflacionar a série com data base = último mês observado
indice_data_base = tabela.query("date == date.max()").igpdi.values[0]
tabela = tabela.assign(real = lambda x: (indice_data_base / x.igpdi) * x.preco)

# Criar índice de datas (já usado no seu dataframe 'tabela')
# Reatribuir frequência semanal explicitamente

#tabela = tabela.set_index('date')

# Criar os termos de Fourier com K = 1
fourier = FourierFeaturizer(m=52, k=1)
_, X_fourier = fourier.fit_transform(tabela.index)

# Assign the date range as a new column
X_fourier['date'] = date_range[:len(df)]

X_fourier= X_fourier.set_index('date')

# Adicionar ao DataFrame principal
tabela = pd.concat([tabela, X_fourier], axis=1)

# Display the merged DataFrame
print(tabela)

```

```{python}
# Visualização de dados

plot =  (p9.ggplot(tabela) +
  p9.aes(x = "date2", y = "real") +
  p9.geom_line() +
  p9.scale_x_date(date_breaks = "2 year", date_labels = "%Y") +
  p9.labs(
    title = "Evolução dos Preços de Manga Palmer: 2012 a 2025",
    subtitle = "Ao produtor do Vale do São Francisco",
    x = "",
    y = "Preços Palmer",
    caption = "Dados: CEPEA reprocessados pelo Observatório de Mercado da Embrapa"
    )
)

plot.show()
```

## Previsão com modelos econométricos

As previsões geradas pelo modelo econométrico ARIMA podem ser conferidas abaixo:

```{python}
# Dados de Clima
clima =pd.read_csv('dados/df_climate_weekly.csv', sep=';', decimal='.')

# Convert 'date' column to datetime format if not already
clima['date'] = pd.to_datetime(clima['date'], dayfirst=True)

# Extract the year from the 'date' column
clima['year'] = clima['date'].dt.year

# Group by 'year' and count the number of observations
observations_per_year = clima.groupby('year').size().reset_index(name='count')

# Display the result
#print(observations_per_year)

# Extrair o ano do índice de datas
tabela['ano'] = tabela.index.year

# Contar número de observações (semanas) por ano
semanas_por_ano = tabela.groupby('ano').size()
#print(semanas_por_ano)

# Junção dos dados

# 1. Remover a coluna 'date'
clima = clima.drop(columns=['date'])

# 2. Atribuir o índice do DataFrame 'tabela' ao DataFrame 'clima'
clima.index = tabela.index

# Junçao das duas bases de dados
tabela_clima = pd.concat([tabela, clima], axis=1)
# Manter apenas as colunas desejadas
tabela_clima = tabela_clima[['real', 'avg_temp', 'total_rainfall', 'FOURIER_S52-0',  'FOURIER_C52-0']]

# Preparação de dados
dados = (
  tabela_clima
  .assign(
    avg_temp_lag16 = lambda x: x.avg_temp.shift(16),
    total_rainfall_lag16 = lambda x: x.total_rainfall.shift(16)
    )
  .dropna()
)

# Separar amostras
# Separar treino e teste
# Separar as últimas 12 observações para teste
dados_teste = dados.tail(12)
dados_treino = dados.iloc[:-12]

# Definir y e X para treino
y_treino = dados_treino['real']
x_treino = dados_treino.drop(columns=['real'])

# Definir y e X para teste
y_teste = dados_teste['real']
x_teste = dados_teste.drop(columns=['real'])
```

```{python}
import warnings
warnings.filterwarnings("ignore")

melhor_aic = float('inf')
melhor_ordem = None
melhor_modelo = None

for p in range(0, 4):
    for d in range(1, 2):  # geralmente 1
        for q in range(0, 4):
            try:
                modelo = SARIMAX(endog=y_treino,
                exog=x_treino, order=(p,d,q), enforce_stationarity=False, 
                enforce_invertibility=False)
                resultado = modelo.fit(disp=False)
                if resultado.aic < melhor_aic:
                    melhor_aic = resultado.aic
                    melhor_ordem = (p, d, q)
                    melhor_modelo = resultado
            except:
                continue

print(f"Melhor modelo: ARIMA{melhor_ordem} com AIC = {melhor_aic:.2f}")

```

```{python}
modelo_sarimax = SARIMAX(
    endog=y_treino,
    exog=x_treino,
    order=(2, 1, 1),
    enforce_stationarity=False,
    enforce_invertibility=False
)

resultado_sarimax = modelo_sarimax.fit(disp=False)

print(resultado_sarimax.summary())
```


```{python}
# Garantir que o índice do exógeno de teste seja numérico sequencial
# para evitar problemas de alinhamento implícito
x_teste_resetado = x_teste.copy()
x_teste_resetado.index = range(len(y_treino), len(y_treino) + len(y_teste))

# Previsão segura
previsao_sarimax = resultado_sarimax.predict(
    start=len(y_treino),
    end=len(y_treino) + len(y_teste) - 1,
    exog=x_teste_resetado
)

# Alinhar com o índice real de y_teste
previsao_sarimax = pd.Series(previsao_sarimax.values, index=y_teste.index)

# Criar DataFrame base com os dados reais
resultados_sarimax = pd.DataFrame(
    data={
        "data": dados.index,
        "y": dados['real'],
    }
)

# Inserir as previsões apenas no período de teste
resultados_sarimax.loc[y_teste.index, 'previsao'] = previsao_sarimax.values

# Gerar gráfico com plotnine
plot = (
    p9.ggplot(resultados_sarimax) +
    p9.aes(x="data", y="y") +
    p9.geom_line(color="black") +
    p9.geom_line(mapping=p9.aes(y="previsao"), color="blue") +
    p9.scale_x_date(date_breaks="2 years", date_labels="%Y") +
    p9.labs(
        title="Previsão SARIMAX para o Preço da Manga Palmer",
        subtitle="Ao produtor do Vale do São Francisco",
        x="",
        y="Preço (R$)",
        caption="Fonte: CEPEA – Observatório de Mercado da Embrapa"
    ) +
    p9.theme_minimal()
)

plot.show()

```

## Previsão com modelos de Machine Learning

As previsões geradas pelo modelo de machine learning XGBoost podem ser conferidas abaixo:

```{python}
# Preparação de dados
dados_xgb = (
  tabela_clima
  .assign(
    real_lag1 = lambda x: x.real.shift(1),
    tendencia = lambda x: range(len(x)),
    avg_temp_lag16 = lambda x: x.avg_temp.shift(16),
    total_rainfall_lag16 = lambda x: x.total_rainfall.shift(16)
    )
  .dropna()
)

# Separar amostras
# Separar treino e teste
# Separar as últimas 12 observações para teste
dados_teste_xgb = dados_xgb.tail(12)
dados_treino_xgb = dados_xgb.iloc[:-12]

# Definir y e X para treino
y_treino_xgb = dados_treino_xgb['real']
x_treino_xgb = dados_treino_xgb.drop(columns=['real'])

# Definir y e X para teste
y_teste_xgb = dados_teste_xgb['real']
x_teste_xgb = dados_teste_xgb.drop(columns=['real'])

# Ensure y_treino has a proper datetime index
y_treino_xgb = pd.Series(y_treino_xgb.values, index=dados_treino_xgb.index)

# Estimar modelo XGBoost
#ajuste_xgboost = xgb.XGBRegressor().fit(X=x_treino, y=y_treino)
ajuste_xgboost = xgb.XGBRegressor(
    n_estimators=100,
    max_depth=3,
    learning_rate=0.1,
    objective="reg:squarederror",
    random_state=42
).fit(X=x_treino_xgb, y=y_treino_xgb)

#Produzir a previsão
previsao_xgboost = pd.Series(
    ajuste_xgboost.predict(x_teste_xgb),
    index=y_teste_xgb.index
)


# Create the DataFrame for plotting
# Align 'previsao_arima' to the correct date range in 'dados'
resultados_xgboost = pd.DataFrame(
  data={
    "data": dados_xgb.index,
    "y": dados_xgb['real'],
  }
)

# Add predictions only for the test period
resultados_xgboost.loc[y_teste_xgb.index, 'previsao'] = previsao_xgboost.values

# Plotting the results
plot = (
  p9.ggplot(resultados_xgboost) +
  p9.aes(x = "data", y = "y") +
  p9.geom_line() +
  p9.geom_line(mapping = p9.aes(y = "previsao"), color = "blue") +
  p9.scale_x_date(date_breaks = "2 years", date_labels = "%Y") +
  p9.labs(
    title = "Previsão XGBOOST para o Preço Manga Palmer",
      subtitle = "Ao produtor do Vale do São Francisco",
    x = "",
    y = "Preços Palmer",
    caption = "Dados: CEPEA reprocessados pelo Observatório de Mercado da Embrapa"
  )
)
plot.show()
```

```{python}
# Criar uma tabela comparativa com as últimas 12 observações
tabela_comparativa = pd.DataFrame({
    'Real': y_teste.tail(12),
    'Previsão_ARIMA': resultados_sarimax.loc[y_teste.tail(12).index, 'previsao'],
    'Previsão_XGBoost': previsao_xgboost.tail(12)
})

# Exibir a tabela
print(tabela_comparativa)
```

```{python}
# Valores reais
y_real = y_teste.tail(12)

# Previsões
y_pred_arima = resultados_sarimax.loc[y_real.index, 'previsao']
y_pred_xgb = previsao_xgboost.tail(12)

# Função auxiliar para MAPE
def mean_absolute_percentage_error(y_true, y_pred):
    return np.mean(np.abs((y_true - y_pred) / y_true)) * 100

# Avaliação SARIMAX
mae_arima = mean_absolute_error(y_real, y_pred_arima)
rmse_arima = np.sqrt(mean_squared_error(y_real, y_pred_arima))
mape_arima = mean_absolute_percentage_error(y_real, y_pred_arima)

# Avaliação XGBoost
mae_xgb = mean_absolute_error(y_real, y_pred_xgb)
rmse_xgb = np.sqrt(mean_squared_error(y_real, y_pred_xgb))
mape_xgb = mean_absolute_percentage_error(y_real, y_pred_xgb)

# Imprimir os resultados
print("=== Avaliação das Previsões ===")
print(f"SARIMAX - MAE: {mae_arima:.2f}, RMSE: {rmse_arima:.2f}, MAPE: {mape_arima:.2f}%")
print(f"XGBoost - MAE: {mae_xgb:.2f}, RMSE: {rmse_xgb:.2f}, MAPE: {mape_xgb:.2f}%")

```

